% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_scores.R
\name{pathway_scores}
\alias{pathway_scores}
\title{Extract Pathway-Level ssGSEA Enrichment Scores}
\usage{
pathway_scores(input_expr = NULL, idType = "SYMBOL")
}
\arguments{
\item{input_expr}{A numeric gene expression matrix (or data frame) with
\strong{genes in rows} and \strong{samples in columns}. Row and column names are
mandatory. Expression values must be non-negative (e.g. TPM, FPKM,
or normalised counts). If the data are not on a log2 scale, they will
be automatically log2-transformed via \code{log2(x + 1)} internally.}

\item{idType}{Character string specifying the gene identifier type used
in the rownames of \code{input_expr}. One of \code{"SYMBOL"} (default),
\code{"ENSEMBL"}, \code{"ENTREZID"}, or \code{"REFSEQ"}. If not \code{"SYMBOL"}, the
function converts rownames to gene symbols via \strong{org.Hs.eg.db}.}
}
\value{
A numeric matrix with \strong{pathway gene sets in rows} and
\strong{samples in columns}. Each value is the ssGSEA enrichment score
for that pathway–sample pair. The pathway names correspond to the
internal \code{required.sets} gene set collection curated for HNSCC
subtype classification.
}
\description{
This function performs input validation, optional gene ID conversion,
log2 transformation (if needed), and single-sample gene set enrichment
analysis (ssGSEA) on a gene expression matrix, then returns the
pathway-level enrichment score matrix. It exposes the intermediate
output that is normally consumed internally by \code{classifyHNSC()}.
}
\details{
Useful for downstream analyses such as pathway-level differential
expression, gene set enrichment analysis (GSEA), clustering, or
custom machine learning experiments using the same pathway
representation that the classifier is built upon.
}
\examples{
\dontrun{
data(TCGA_LUSC)
pw <- pathway_scores(TCGA_LUSC)
head(pw[, 1:5])            # first 5 samples, all pathways

# With Ensembl IDs
pw_ens <- pathway_scores(TCGA_LUSC_ENSEMBL, idType = "ENSEMBL")
}
}
\seealso{
\code{\link{classifyHNSC}} for the full classification
pipeline, including the random forest step applied downstream of
these scores.
}
