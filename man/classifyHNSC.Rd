% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifyHNSC.R
\name{classifyHNSC}
\alias{classifyHNSC}
\title{Classify Head and Neck Squamous Cell Carcinoma Samples into TCGA Subtypes}
\usage{
classifyHNSC(
  input_expr = NULL,
  idType = "SYMBOL",
  outputType = c("class", "prob")
)
}
\arguments{
\item{input_expr}{A numeric gene expression matrix (or data frame) with
\strong{genes in rows} and \strong{samples in columns}. Row and column names are
mandatory. The expression values must be non-negative (e.g., TPM, FPKM,
or normalized counts). If the data are not on a log2 scale, they will be
automatically log2-transformed via \code{log2(x + 1)} internally.}

\item{idType}{Character string specifying the gene identifier type used in
the rownames of \code{input_expr}. Acceptable values are \code{"SYMBOL"} (default),
\code{"ENSEMBL"}, \code{"ENTREZID"}, or \code{"REFSEQ"}. If any type other than
\code{"SYMBOL"} is provided, the function will convert the rownames to gene
symbols using the \strong{org.Hs.eg.db} annotation package. This requires
\strong{org.Hs.eg.db} and \strong{AnnotationDbi} to be installed (they are listed
under \code{Suggests} of the package). For multiple identifiers mapping to the
same gene symbol, expression values are averaged.}

\item{outputType}{Character indicating the type of prediction result to
return. Use \code{"class"} (default) to obtain the assigned subtype label for
each sample, or \code{"prob"} to get a matrix of class probabilities (one row
per sample, one column per subtype).}
}
\value{
\itemize{
\item If \code{outputType = "class"}, a named character vector of predicted
subtypes (e.g., "Atypical", "Basal", "Classical", "Mesenchymal").
\item If \code{outputType = "prob"}, a numeric matrix with samples in rows,
subtypes in columns, and values ranging from 0 to 1 representing the
predicted probability of each subtype.
}
}
\description{
This function takes a gene expression matrix, performs quality checks,
optional log2 transformation, single-sample gene set enrichment (ssGSEA)
at the pathway level, and then predicts the TCGA-based molecular subtype
of each sample using a pre-trained random forest model.
}
\details{
The function proceeds through five main steps:
\enumerate{
\item \strong{Input validation}: Checks for missing values (\code{NA}), non-numeric
columns, negative expression values, missing row/column names, and
correct sample number.
\item \strong{Gene ID conversion} (if \code{idType} is not \code{"SYMBOL"}):
Converts row identifiers to gene symbols using \code{org.Hs.eg.db}.
If multiple identifiers map to the same gene symbol, the maximum
expression value is retained for each sample.
\item \strong{Log2 transformation}: If the data are not already on a log2 scale
(determined by an internal heuristic), they are transformed as
\code{log2(x + 1)}.
\item \strong{Pathway-level representation}: Single-sample gene set enrichment
analysis (ssGSEA) is performed using a curated set of pathway gene sets
stored internally as \code{required.sets}. This step harmonizes the input
data with the training cohort at the biological pathway level.
\item \strong{Random forest classification}: The pathway scores are centered,
scaled, and fed into a pre-trained random forest model (\code{finalModel}) to
generate the final subtype prediction.
}
Both \code{required.sets} and \code{finalModel} are internal package data and are
loaded automatically. The function requires the \pkg{GSVA} package.
}
\note{
If using \code{idType} values other than \code{"SYMBOL"}, the packages
\strong{org.Hs.eg.db} and \strong{AnnotationDbi} must be installed. You can install
them with:
\preformatted{
BiocManager::install("org.Hs.eg.db")
BiocManager::install("AnnotationDbi")
}
The conversion step may remove genes that lack a unique symbol mapping;
a warning will indicate the number of genes retained.
}
\examples{
\dontrun{
# Example with gene symbols (default)
data(TCGA_LUSC)
subtypes <- classifyHNSC(TCGA_LUSC, outputType = "class")

# Example with Ensembl IDs
# Ensure org.Hs.eg.db is installed
subtypes_ens <- classifyHNSC(TCGA_LUSC_ENSEMBL,
                             idType = "ENSEMBL",
                             outputType = "prob")
}
}
\seealso{
\code{\link[GSVA]{gsva}} for the underlying pathway scoring engine.
}
